import type { MenuKey } from "../types/domain";

export const pageRouteMap: Record<MenuKey, string> = {
  overview: "overview",
  products: "products",
  modules: "modules",
  versions: "versions",
  requirements: "requirements",
  tasks: "tasks",
  reports: "reports",
  dicts: "dicts",
  settings: "settings",
  menus: "menus"
};
